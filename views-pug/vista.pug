head
    style 
        include estilos.css
div(class="jumbotron")
    h1 "Vista de Productos"
    if hayProductos
        div(class="table-responsive")
            table(class="table table-dark")
                tr 
                    th Nombre 
                    th Precio 
                    th Foto
                    th Id
                each producto in productos
                    tr 
                        td=producto.title
                        td=producto.price
                        td 
                            img(width="50" src=producto.thumbnail alt="not found")
                        td=producto.id
    else 
        h3(class="btn btn-outline-danger") no se encontraron productos
   


